import * as React from "react";

interface Props {
  manager: any;
  model: any;
}

export default class Renderer extends React.Component<Props> {
  render() {
    return <pre>Renderer implementation is in progress.</pre>;
  }
}
